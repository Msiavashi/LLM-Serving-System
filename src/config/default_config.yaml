redis:
  host: localhost
  port: 6379
  db: 0
  password: null
  ssl: false
  socket_timeout: 5
  socket_connect_timeout: 5
  retry_on_timeout: true
  max_connections: 10

model:
  type: mixtral
  params: {}
  rank: 1
  checkpoint: mistralai/Mixtral-8x7B-Instruct-v0.1

server:
  host: 0.0.0.0
  port: 8000

scheduler:
  type: async_fcfs
  batch_size: 32

engine:
  type: async_model
