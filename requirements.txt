# QLLM - Core Dependencies
# Tested with Python 3.10, CUDA 12.4, A100/H100/L40 GPUs

# PyTorch and CUDA
torch>=2.5.0,<3.0.0
triton>=3.1.0

# Transformers ecosystem
# Pinned to 4.x: QLLM subclasses internal model classes (MixtralForCausalLM,
# PhimoeForCausalLM, LlamaForCausalLM) which may break in transformers 5.x
transformers>=4.46.0,<5.0.0
accelerate>=1.0.0
tokenizers>=0.20.0
safetensors>=0.4.0
huggingface-hub>=0.25.0

# Quantization
bitsandbytes>=0.44.0

# Flash Attention (optional but recommended — requires CUDA GPU to build)
# Install separately if build fails: pip install flash-attn --no-build-isolation
flash-attn>=2.7.0

# Numerical
numpy>=1.26.0,<2.0.0

# API Server
fastapi>=0.115.0
uvicorn>=0.34.0
pydantic>=2.0.0

# Queue backends
redis>=5.0.0

# Client (for async inference examples)
openai>=1.0.0

# Utilities
tqdm>=4.66.0
PyYAML>=6.0.0
requests>=2.32.0
filelock>=3.16.0
packaging>=24.0
regex>=2024.9.0
sympy>=1.13.0
typing_extensions>=4.12.0

# Monitoring
nvtx>=0.2.10
psutil>=5.9.0

# Optional: Distributed inference (requires system MPI installation)
# mpi4py>=4.0.0

# Optional: Text Generation Inference client (for TGI example)
# text-generation>=0.7.0
